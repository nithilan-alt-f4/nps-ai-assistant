# Node
node_modules/

# Secrets & env
.env
.env.*
!*.env.example

# OAuth tokens & sessions
nps-session.json
gmail-tokens.json
gmail-classify-cache.json
calendar-tokens.json
*.session.json

# Aakash bot data (has WhatsApp session + creds)
aakash-cal-bot/
data/
wa-session/

# Old WhatsApp/browser session profiles (may contain creds) - never public
backend/_legacy/test-wa-session/
backend/_legacy/whatsapp-session/

# Legacy prototype folder (contains hardcoded API keys) - keep out of public repo
backend/_legacy/

# Logs
*.log
server.log
server_err.log

# OS
Thumbs.db
.DS_Store

# Media / downloads (regenerated by sync)
media/
downloads/
backend/media/
backend/downloads/

# Local probe scripts
probe*.mjs
